<UserControl
    x:Class="outphasing_pa_test_equipment_controller.VisaDebugger"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:outphasing_pa_test_equipment_controller"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="50"
    d:DesignWidth="500"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button
            x:Name="WriteButton"
            Grid.Row="0"
            Grid.Column="0"
            Click="WriteButton_Click"
            Content="Write" />
        <Button
            x:Name="QueryButton"
            Grid.Row="1"
            Grid.Column="0"
            Click="QueryButton_Click"
            Content="Query" />
        <TextBox
            x:Name="WriteTextBox"
            Grid.Row="0"
            Grid.RowSpan="2"
            Grid.Column="1" />
        <TextBlock
            x:Name="ReadTextBlock"
            Grid.Row="0"
            Grid.RowSpan="2"
            Grid.Column="2" />
    </Grid>
</UserControl>
