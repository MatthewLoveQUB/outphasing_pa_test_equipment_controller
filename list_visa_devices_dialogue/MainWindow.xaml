<Window x:Class="list_visa_devices_dialogue.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:list_visa_devices_dialogue"
        mc:Ignorable="d"
        Title="VISA Address Search Tool" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="AddressListBox" 
                 ItemsSource="{Binding AddressList}" 
                 Grid.Row="0" 
                 SelectionChanged="AddressListBox_SelectionChanged">
        </ListBox>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Click="GetVisaAddressesButton_Click" Grid.Column="0" FontSize="18">
                Get Addresses
            </Button>
            <Button Click="ChooseVisaAddressButton_Click" Grid.Column="1" FontSize="18">
                Ok
            </Button>
        </Grid>
    </Grid>
</Window>
